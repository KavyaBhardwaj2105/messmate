import React,{useState} from 'react';
import {Link} from 'react-router-dom';
import {Mail,Loader2} from 'lucide-react';
import PageContainer from '../components/layout/PageContainer';
import {authService} from '../services/api';
export default function ForgotPasswordPage(){const[email,setEmail]=useState('');const[loading,setLoading]=useState(false);const[message,setMessage]=useState('');const[error,setError]=useState('');const submit=async e=>{e.preventDefault();setLoading(true);setError('');try{const r=await authService.forgotPassword(email.trim());setMessage(r.data.message)}catch(e){setError(e.message)}finally{setLoading(false)}};return <PageContainer maxWidth="max-w-md"><div className="bg-white rounded-3xl p-8 border border-slate-200 shadow-lifted"><h1 className="text-2xl font-black text-slate-900">Reset your password</h1><p className="text-sm text-slate-500 mt-1 mb-6">We’ll email a secure reset link if the account exists.</p>{message&&<div className="mb-5 p-3 rounded-xl bg-emerald-50 border border-emerald-200 text-xs font-semibold text-emerald-800">{message}</div>}{error&&<div className="mb-5 p-3 rounded-xl bg-rose-50 border border-rose-200 text-xs font-semibold text-rose-800">{error}</div>}<form onSubmit={submit} className="space-y-4"><div className="relative"><Mail className="w-5 h-5 absolute left-3 top-1/2 -translate-y-1/2 text-slate-400"/><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" className="w-full pl-11 py-3 rounded-2xl border border-slate-200 bg-slate-50"/></div><button disabled={loading} className="w-full py-3 rounded-2xl bg-brand-600 text-white font-bold flex items-center justify-center gap-2">{loading?<Loader2 className="w-4 h-4 animate-spin"/>:'Send reset link'}</button></form><Link to="/login" className="block text-center mt-5 text-xs font-bold text-brand-600">Back to sign in</Link></div></PageContainer>}
