import React,{useState} from 'react';
import {Link,useNavigate,useSearchParams} from 'react-router-dom';
import {Lock,Loader2} from 'lucide-react';
import PageContainer from '../components/layout/PageContainer';
import {authService} from '../services/api';
export default function ResetPasswordPage(){const[params]=useSearchParams();const token=params.get('token')||'';const[password,setPassword]=useState('');const[confirm,setConfirm]=useState('');const[loading,setLoading]=useState(false);const[error,setError]=useState('');const navigate=useNavigate();const submit=async e=>{e.preventDefault();if(password.length<8)return setError('Password must be at least 8 characters.');if(password!==confirm)return setError('Passwords do not match.');setLoading(true);setError('');try{await authService.resetPassword(token,password);navigate('/login',{replace:true,state:{verificationRequired:false}})}catch(e){setError(e.message)}finally{setLoading(false)}};return <PageContainer maxWidth="max-w-md"><div className="bg-white rounded-3xl p-8 border border-slate-200 shadow-lifted"><h1 className="text-2xl font-black text-slate-900">Choose a new password</h1><p className="text-sm text-slate-500 mt-1 mb-6">Use at least 8 characters.</p>{error&&<div className="mb-5 p-3 rounded-xl bg-rose-50 border border-rose-200 text-xs font-semibold text-rose-800">{error}</div>}<form onSubmit={submit} className="space-y-4"><div className="relative"><Lock className="w-5 h-5 absolute left-3 top-1/2 -translate-y-1/2 text-slate-400"/><input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} placeholder="New password" className="w-full pl-11 py-3 rounded-2xl border border-slate-200 bg-slate-50"/></div><div className="relative"><Lock className="w-5 h-5 absolute left-3 top-1/2 -translate-y-1/2 text-slate-400"/><input type="password" minLength={8} required value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Confirm password" className="w-full pl-11 py-3 rounded-2xl border border-slate-200 bg-slate-50"/></div><button disabled={loading||!token} className="w-full py-3 rounded-2xl bg-brand-600 text-white font-bold flex items-center justify-center gap-2">{loading?<Loader2 className="w-4 h-4 animate-spin"/>:'Reset password'}</button></form><Link to="/login" className="block text-center mt-5 text-xs font-bold text-brand-600">Back to sign in</Link></div></PageContainer>}
